import React from 'react';
import { findByText, fireEvent, getByTestId } from '@testing-library/dom';
import RemovableTag, { SimpleTag } from '../../src';
export default {
  title: 'Examples'
};
export const simpleTag = () => /*#__PURE__*/React.createElement(SimpleTag, {
  text: "simple tag"
});
export const removableTag = () => /*#__PURE__*/React.createElement(RemovableTag, {
  text: "removable tag",
  testId: "test-tag",
  isRemovable: true
});
const interactionTasks = [{
  name: 'RemovableTag',
  description: 'Render a tag and then remove it',
  run: async ({
    container
  }) => {
    await findByText(container, 'removable tag', undefined, {
      timeout: 2000
    });
    const button = getByTestId(container, 'close-button-test-tag');
    fireEvent.click(button);
  }
}];
removableTag.story = {
  name: 'RemovableTag',
  parameters: {
    performance: {
      interactions: interactionTasks
    }
  }
};