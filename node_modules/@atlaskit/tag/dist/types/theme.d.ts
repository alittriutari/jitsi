import { ThemeModes } from '@atlaskit/theme/types';
import { ChromeColors, ChromeLinkColors, TagColor } from './types';
export declare const getThemeColors: (color: TagColor, mode: ThemeModes) => {
    chromeColors: ChromeColors;
    chromeLinkColors: ChromeLinkColors;
    buttonColors: {
        backgroundColor: string;
        backgroundColorHover: string;
        focusBoxShadowColor: string;
        hoverBoxShadowColor: string;
    };
    linkHoverColor: string;
};
